# axis: terrain  (PMT plan §2, §7 source: terrain_flat_mix_env_cfg.py)
# Combined big-map + flat-patch mesh: the original big-map terrain plus a flat slab
# added at world (90, 0, 0) (beyond the [-60, 60] big-map bounds, no z-fighting) by
# scripts/motion_adapter/add_flat_patch_to_stl.py. Terrain clips are anchored to the
# big-map region; flat clips are shifted onto the flat patch via per-clip env-origin
# injection (flat_origin in motion/grouped_terrain_flat.yaml == this patch's (90,0,0)).
type: terrain_flat_mix
mesh_path: ${paths.TERRAIN_ROOT}/g1_29dof_big_map_with_flat.stl
# Sensor geometry (height-scan grid + contact sensor) is baked in the ported scene cfg
# (pmt_tasks/env_cfgs/terrain_flat_mix.py reuses SteppingStoneSceneCfg); lifting it to
# YAML is a Phase-2 parametrization TODO (plan §7).
