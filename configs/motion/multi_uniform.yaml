# axis: motion  (PMT plan §2 — sampler is a MOTION-axis param, not a new env class)
# Eager multi-clip tracking with the UNIFORM sampler (no curriculum). Mirrors the old
# G1MultiMotionV2UniformEnvCfg (sampler_type="uniform", multi_motion_env_cfg.py:288).
command_class: MultiMotionCommandV2
motion_files: ${paths.MOTION_ROOT}/lafan_walk
terrain_motion_files: null
sampler: uniform
storage_mode: eager
chunk_length: 4000
decimation: 4
sim_dt: 0.005
